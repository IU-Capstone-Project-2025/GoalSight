module.exports = {
    plugins: {
        // Tailwind CSS plugin — enables utility-first styling and processing of Tailwind classes
        tailwindcss: {},

        // Autoprefixer plugin — automatically adds vendor prefixes to CSS for better browser compatibility
        autoprefixer: {},
    }
}