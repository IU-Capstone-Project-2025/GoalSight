// Jest file mock: used to mock static file imports (images, fonts, media) in tests
// When a test imports a file (e.g., .png, .svg), Jest will use this stub instead of the real file
// This prevents errors when importing non-JS assets in test environments
module.exports = 'test-file-stub'; 